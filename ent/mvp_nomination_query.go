// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/bengobox/game-stats-api/ent/mvp_nomination"
	"github.com/bengobox/game-stats-api/ent/player"
	"github.com/bengobox/game-stats-api/ent/predicate"
	"github.com/bengobox/game-stats-api/ent/spiritscore"
	"github.com/google/uuid"
)

// MVPNominationQuery is the builder for querying MVP_Nomination entities.
type MVPNominationQuery struct {
	config
	ctx             *QueryContext
	order           []mvp_nomination.OrderOption
	inters          []Interceptor
	predicates      []predicate.MVP_Nomination
	withSpiritScore *SpiritScoreQuery
	withPlayer      *PlayerQuery
	withFKs         bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the MVPNominationQuery builder.
func (_q *MVPNominationQuery) Where(ps ...predicate.MVP_Nomination) *MVPNominationQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *MVPNominationQuery) Limit(limit int) *MVPNominationQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *MVPNominationQuery) Offset(offset int) *MVPNominationQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *MVPNominationQuery) Unique(unique bool) *MVPNominationQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *MVPNominationQuery) Order(o ...mvp_nomination.OrderOption) *MVPNominationQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QuerySpiritScore chains the current query on the "spirit_score" edge.
func (_q *MVPNominationQuery) QuerySpiritScore() *SpiritScoreQuery {
	query := (&SpiritScoreClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(mvp_nomination.Table, mvp_nomination.FieldID, selector),
			sqlgraph.To(spiritscore.Table, spiritscore.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, mvp_nomination.SpiritScoreTable, mvp_nomination.SpiritScoreColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryPlayer chains the current query on the "player" edge.
func (_q *MVPNominationQuery) QueryPlayer() *PlayerQuery {
	query := (&PlayerClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(mvp_nomination.Table, mvp_nomination.FieldID, selector),
			sqlgraph.To(player.Table, player.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, mvp_nomination.PlayerTable, mvp_nomination.PlayerColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first MVP_Nomination entity from the query.
// Returns a *NotFoundError when no MVP_Nomination was found.
func (_q *MVPNominationQuery) First(ctx context.Context) (*MVP_Nomination, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{mvp_nomination.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *MVPNominationQuery) FirstX(ctx context.Context) *MVP_Nomination {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first MVP_Nomination ID from the query.
// Returns a *NotFoundError when no MVP_Nomination ID was found.
func (_q *MVPNominationQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{mvp_nomination.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *MVPNominationQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single MVP_Nomination entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one MVP_Nomination entity is found.
// Returns a *NotFoundError when no MVP_Nomination entities are found.
func (_q *MVPNominationQuery) Only(ctx context.Context) (*MVP_Nomination, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{mvp_nomination.Label}
	default:
		return nil, &NotSingularError{mvp_nomination.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *MVPNominationQuery) OnlyX(ctx context.Context) *MVP_Nomination {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only MVP_Nomination ID in the query.
// Returns a *NotSingularError when more than one MVP_Nomination ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *MVPNominationQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{mvp_nomination.Label}
	default:
		err = &NotSingularError{mvp_nomination.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *MVPNominationQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of MVP_Nominations.
func (_q *MVPNominationQuery) All(ctx context.Context) ([]*MVP_Nomination, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*MVP_Nomination, *MVPNominationQuery]()
	return withInterceptors[[]*MVP_Nomination](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *MVPNominationQuery) AllX(ctx context.Context) []*MVP_Nomination {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of MVP_Nomination IDs.
func (_q *MVPNominationQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(mvp_nomination.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *MVPNominationQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *MVPNominationQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*MVPNominationQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *MVPNominationQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *MVPNominationQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *MVPNominationQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the MVPNominationQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *MVPNominationQuery) Clone() *MVPNominationQuery {
	if _q == nil {
		return nil
	}
	return &MVPNominationQuery{
		config:          _q.config,
		ctx:             _q.ctx.Clone(),
		order:           append([]mvp_nomination.OrderOption{}, _q.order...),
		inters:          append([]Interceptor{}, _q.inters...),
		predicates:      append([]predicate.MVP_Nomination{}, _q.predicates...),
		withSpiritScore: _q.withSpiritScore.Clone(),
		withPlayer:      _q.withPlayer.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithSpiritScore tells the query-builder to eager-load the nodes that are connected to
// the "spirit_score" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *MVPNominationQuery) WithSpiritScore(opts ...func(*SpiritScoreQuery)) *MVPNominationQuery {
	query := (&SpiritScoreClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withSpiritScore = query
	return _q
}

// WithPlayer tells the query-builder to eager-load the nodes that are connected to
// the "player" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *MVPNominationQuery) WithPlayer(opts ...func(*PlayerQuery)) *MVPNominationQuery {
	query := (&PlayerClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withPlayer = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Category string `json:"category,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.MVPNomination.Query().
//		GroupBy(mvp_nomination.FieldCategory).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *MVPNominationQuery) GroupBy(field string, fields ...string) *MVPNominationGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &MVPNominationGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = mvp_nomination.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Category string `json:"category,omitempty"`
//	}
//
//	client.MVPNomination.Query().
//		Select(mvp_nomination.FieldCategory).
//		Scan(ctx, &v)
func (_q *MVPNominationQuery) Select(fields ...string) *MVPNominationSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &MVPNominationSelect{MVPNominationQuery: _q}
	sbuild.label = mvp_nomination.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a MVPNominationSelect configured with the given aggregations.
func (_q *MVPNominationQuery) Aggregate(fns ...AggregateFunc) *MVPNominationSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *MVPNominationQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !mvp_nomination.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *MVPNominationQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*MVP_Nomination, error) {
	var (
		nodes       = []*MVP_Nomination{}
		withFKs     = _q.withFKs
		_spec       = _q.querySpec()
		loadedTypes = [2]bool{
			_q.withSpiritScore != nil,
			_q.withPlayer != nil,
		}
	)
	if _q.withSpiritScore != nil || _q.withPlayer != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, mvp_nomination.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*MVP_Nomination).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &MVP_Nomination{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withSpiritScore; query != nil {
		if err := _q.loadSpiritScore(ctx, query, nodes, nil,
			func(n *MVP_Nomination, e *SpiritScore) { n.Edges.SpiritScore = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withPlayer; query != nil {
		if err := _q.loadPlayer(ctx, query, nodes, nil,
			func(n *MVP_Nomination, e *Player) { n.Edges.Player = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *MVPNominationQuery) loadSpiritScore(ctx context.Context, query *SpiritScoreQuery, nodes []*MVP_Nomination, init func(*MVP_Nomination), assign func(*MVP_Nomination, *SpiritScore)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*MVP_Nomination)
	for i := range nodes {
		if nodes[i].spirit_score_mvp_nominations == nil {
			continue
		}
		fk := *nodes[i].spirit_score_mvp_nominations
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(spiritscore.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "spirit_score_mvp_nominations" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *MVPNominationQuery) loadPlayer(ctx context.Context, query *PlayerQuery, nodes []*MVP_Nomination, init func(*MVP_Nomination), assign func(*MVP_Nomination, *Player)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*MVP_Nomination)
	for i := range nodes {
		if nodes[i].player_mvp_nominations == nil {
			continue
		}
		fk := *nodes[i].player_mvp_nominations
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(player.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "player_mvp_nominations" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (_q *MVPNominationQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *MVPNominationQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(mvp_nomination.Table, mvp_nomination.Columns, sqlgraph.NewFieldSpec(mvp_nomination.FieldID, field.TypeUUID))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, mvp_nomination.FieldID)
		for i := range fields {
			if fields[i] != mvp_nomination.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *MVPNominationQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(mvp_nomination.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = mvp_nomination.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// MVPNominationGroupBy is the group-by builder for MVP_Nomination entities.
type MVPNominationGroupBy struct {
	selector
	build *MVPNominationQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *MVPNominationGroupBy) Aggregate(fns ...AggregateFunc) *MVPNominationGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *MVPNominationGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*MVPNominationQuery, *MVPNominationGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *MVPNominationGroupBy) sqlScan(ctx context.Context, root *MVPNominationQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// MVPNominationSelect is the builder for selecting fields of MVPNomination entities.
type MVPNominationSelect struct {
	*MVPNominationQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *MVPNominationSelect) Aggregate(fns ...AggregateFunc) *MVPNominationSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *MVPNominationSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*MVPNominationQuery, *MVPNominationSelect](ctx, _s.MVPNominationQuery, _s, _s.inters, v)
}

func (_s *MVPNominationSelect) sqlScan(ctx context.Context, root *MVPNominationQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
